# JS SDK build + deps
js/node_modules/
js/dist/
# NOTE: package-lock.json IS committed — `npm ci` (CI + release) requires it.

# Python SDK build + caches
python/build/
python/dist/
python/*.egg-info/
python/**/__pycache__/
python/.pytest_cache/
